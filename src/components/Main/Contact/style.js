import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    container: {
        display: "flex",
        flexDirection: "column",
        rowGap: "15px",
    },

    socmedContainer: {
        display: "flex",
        columnGap: "10px",
        alignItems: "center",
    },
}))